{
  "name": "ahrjarrett.com",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "./node_modules/.bin/lerna exec -- npm run build"
  },
  "devDependencies": {
    "lerna": "^3.13.1"
  }
}
