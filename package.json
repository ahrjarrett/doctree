{
  "name": "ahrjarrett",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "./node_modules/.bin/lerna exec -- npm run build"
  },
  "devDependencies": {
    "@babel/plugin-transform-modules-commonjs": "7.2.0",
    "lerna": "^3.13.1"
  }
}
